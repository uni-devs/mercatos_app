import 'package:flutter/material.dart';

class StyledBottomWidget extends StatelessWidget {
  const StyledBottomWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {},
      child: Text(""),
    );
  }
}
